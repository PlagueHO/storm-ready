name: Chore request
description: Request a maintenance task that doesn't add user-facing features.
labels: [chore]
body:
  - type: textarea
    id: task
    attributes:
      label: What needs to be done?
    validations:
      required: true
  - type: textarea
    id: reason
    attributes:
      label: Why is this needed?
